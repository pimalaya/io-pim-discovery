name: Releases

on:
  push:
    tags:
      - v*
    branches:
      - master

jobs:
  release:
    uses: pimalaya/nix/.github/workflows/releases.yml@master
    secrets: inherit
    with:
      project: pim-discovery
      nixpkgs: nixos-25.11
